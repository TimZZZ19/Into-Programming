public class replaceStr {
    
    public static void main(String[] args) {
        // Your code here
        // Consider the method replaceStr, which is instended to 
        // return a string that is identical to the original string
        // except when there are occurrences of some character, in
        // which case all those occurrences will be repaced by another
        // give character. 
	int[] arr = {1, 2, 3, 4};
	for (int i = 0; i < arr.length; i++){
		System.out.print(arr[i]);
	}
	int count = 0;
	while (count < 5){
		System.out.println(count);
		count++;
	}

    }
}

